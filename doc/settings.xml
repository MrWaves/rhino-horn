<?xml version="1.0" encoding="UTF-8"?>
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 http://maven.apache.org/xsd/settings-1.0.0.xsd">
<localRepository>D:\developSoft\MavenRepository</localRepository>
<pluginGroups>
</pluginGroups>
<proxies>
</proxies>
<servers>
    <!-- server
     | Specifies the authentication information to use when connecting to a particular server, identified by
     | a unique name within the system (referred to by the 'id' attribute below).
     | 
     | NOTE: You should either specify username/password OR privateKey/passphrase, since these pairings are 
     |       used together.
     |
    <server>
      <id>deploymentRepo</id>
      <username>repouser</username>
      <password>repopwd</password>
    </server>
    -->
    
    <!-- Another sample, using keys to authenticate.
    <server>
      <id>siteServer</id>
      <privateKey>/path/to/private/key</privateKey>
      <passphrase>optional; leave empty if not used.</passphrase>
    </server>
    -->
	<server>
		<id>releases</id>
		<username>deployment</username>
		<password>123456</password>
	</server>
	<server>
		<id>snapshots</id>
		<username>deployment</username>
		<password>123456</password>
	</server>
  </servers>
  
    <mirrors>
    <!-- mirror
     | Specifies a repository mirror site to use instead of a given repository. The repository that
     | this mirror serves has an ID that matches the mirrorOf element of this mirror. IDs are used
     | for inheritance and direct lookup purposes, and must be unique across the set of mirrors.
     |
	 -->
	<mirror>
      <id>nexus</id>
      <url>http://nexus.corp.rs.com/nexus/content/groups/public/</url>
	  <mirrorOf>*</mirrorOf>
    </mirror>
    <mirror>
      <id>releases</id>
      <url>http://nexus.corp.rs.com/nexus/content/repositories/releases/</url>
	  <mirrorOf>releases</mirrorOf>
    </mirror>
    <mirror>
      <id>snapshots</id>
      <url>http://nexus.corp.rs.com/nexus/content/repositories/snapshots/</url>
	  <mirrorOf>snapshots</mirrorOf>
    </mirror>
    <mirror>
      <id>3rd party</id>
      <url>http://nexus.corp.rs.com/nexus/content/repositories/snapshots/</url>
    <mirrorOf>3rd party</mirrorOf>
    </mirror>  
  </mirrors>
    <profiles>
        <profile>
            <id>redstar-nexus</id>
            <repositories>
                <repository>
                    <id>nexus_release_repository</id>
                    <name>nexus_release_repository</name>
                    <releases>
                        <enabled>true</enabled>
                    </releases>
                    <snapshots>
                        <enabled>false</enabled>
                    </snapshots>
                    <url>http://nexus.corp.rs.com/nexus/service/local/repositories/releases/content</url>
                    <layout>default</layout>
                </repository>
                <repository>
                    <id>nexus_snapshot_repository</id>
                    <name>nexus_repository</name>
                    <releases>
                        <enabled>false</enabled>
                    </releases>
                    <snapshots>
                        <enabled>true</enabled>
                    </snapshots>
                    <url>http://nexus.corp.rs.com/nexus/content/repositories/snapshots</url>
                    <layout>default</layout>
                </repository>
            </repositories>
        </profile>
    </profiles>
    <activeProfiles>
        <activeProfile>redstar-nexus</activeProfile>
    </activeProfiles>
</settings>